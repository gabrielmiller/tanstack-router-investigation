{
  "name": "react-tanstack-router",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx browser-sync start --server --files \"src/*\" --no-open --no-notify --directory"
  },
  "author": "",
  "license": "WTFPL"
}
